在函数声明中，还可以包含多维数组。三维数组参数的格式为：

 
base_type[][depth][depth]


例如，具有多维数组作为参数的函数可以是： 

 
void procedure (int myarray[][3][4])


请注意，第一个括号[]保留为空，而以下括号指定各自尺寸的大小。这是必要的，以便编译器能够确定每个附加维度的深度。

在某种程度上，将数组作为参数传递总是会丢失一个维度。其背后的原因是，由于历史原因，数组不能直接复制，因此实际传递的是指针。
这是新手程序员常见的错误来源。